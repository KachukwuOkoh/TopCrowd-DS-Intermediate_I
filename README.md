- Download the zipped file from here or clone the github repo using your preferred terminal.

- Once on your local machine, create a virtual environment on any current directory from your terminal using the command: `python -m venv .`

- Install all the libraries needed from the requirements.txt file from your terminal using the command: `pip install -r requirements.txt`

- Navigate to the `prediction.ipynb` file and click on `Run all`

- Inspect output and metrics of different Machine Learning methods used with justifications.



Thnak You!